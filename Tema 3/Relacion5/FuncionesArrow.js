const sum = (num1, num2) => num1 + num2;
const stringLength = str => "El tamaño de la cadena es: " + str.length;
const stringLength2 = str => "El tamaño de la cadena es: " + str.length;
const showAlert = (name) => alert(alerts[Math.floor(Math.random() * alerts.length)] + `, ${name}!`);